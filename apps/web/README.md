# NexPod Web Dashboard

Reserved for Antigravity. Build the dashboard against mock-data adapters only; keep all fetching behind a single data-access boundary so it can later call the API at `/api/v1/dashboard` without rewriting UI components.
